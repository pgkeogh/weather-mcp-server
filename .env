# Azure Key Vault Configuration (REQUIRED for enterprise security)
KEY_VAULT_NAME=pgk-key-vault

# Model Configuration (NOT secrets - safe in environment)
OPENAI_MODEL=gpt-4o-mini
MAX_TOKENS=1000
TEMPERATURE=0.7

# Connection Configuration (NOT secrets - safe in environment)
CONNECTION_TIMEOUT=30
RETRY_ATTEMPTS=3
LOG_LEVEL=INFO

# Note: API keys and other secrets are stored in Azure Key Vault only
# Ensure the 'openai-api-key' secret exists in your Key Vault